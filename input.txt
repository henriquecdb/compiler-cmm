int main(){
    int a, b, res_int;
    float x, y, res_float;

    a = 5;
    b = 2;
    x = 10.5;
    y = 2.0;

    res_float = x + a * y - (b / 2.0);
    res_int = (a % b) * 3 + x;

    print(res_int);
    print(res_float);

    return 0;
}